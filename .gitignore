# Firebase service account key — ห้าม commit โดยเด็ดขาด
setup/serviceAccountKey.json

# CSV ที่มีข้อมูลผู้เข้าร่วม (รหัสผ่าน / UID)
setup/*.csv

# Node modules
setup/node_modules/

# Local env / config
setup/.env
setup/config.local.js

# ไกด์ไลน์เวชปฏิบัติ — มีลิขสิทธิ์ + ไฟล์ใหญ่ ห้าม commit
# manifest.json สำรองไว้ที่ DOC/guidelines/manifest.json (เขียนด้วยมือ กำหนด corpusVersion)
setup/guidelines/

# Playwright
node_modules/
test-results/
playwright-report/
tests/.env.test

# OS
.DS_Store
Thumbs.db
